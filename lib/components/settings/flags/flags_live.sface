  {#if !Bonfire.Boundaries.can?(@__context__, :mediate, @category)}
    {l("Sorry, you cannot edit this group.")}
  {#else}
<Dynamic.LiveComponent
  :if={module_enabled?(Bonfire.UI.Social.FlagsLive)}
  module={Bonfire.UI.Social.FlagsLive}
  selected_tab={@selected_tab}
  id="group_flags"
  scope={@category}
/>
{/if}