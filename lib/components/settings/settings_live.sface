<Bonfire.UI.Common.TabsLive
  selected_tab={@selected_tab}
  path_prefix={"/group/#{e(@group, :character, :username, "")}/settings/"}
  tabs={Config.get([:ui, :group, :settings, :navigation], general: l("General"))}
/>

<Dynamic.Component
    module={Config.get([:ui, :group, :settings, :sections, tab(@selected_tab)], Bonfire.UI.Groups.Settings.GeneralLive)}
    {...assigns}
  >
</Dynamic.Component>